@model OrderDetailsPastViewModel

<div class="container" style="min-height:525px">
    <div class="row">
        <div class="col-md-12 order-md-2 mb-4">
            <h4 class="d-flex justify-content-between align-items-center mb-3 pt-5">
                <span class="text-muted">Your orders</span>
                <span class="badge badge-secondary badge-pill">@Model.Count</span>
            </h4>
            <table class="table table-light">
                <tr>
                    <th scope="col">Order Id</th>
                    <th scope="col">Package Name</th>
                    <th scope="col">Date</th>
                    <th scope="col"></th>
                </tr>
                @foreach (var item in Model.Orders)
                {

                    <tr>
                        <td>@item.OrderId</td>
                        <td>@item.PackageName</td>
                        <td>@item.Date.ToShortDateString()</td>
                        <td><a class="btn btn-primary" href="@Url.Action("Create","Feedback", new { id = item.PackageId})">Leave feedback</a></td>
                    </tr>
                }
            </table>
            @if (Model.Orders.Count() == 0)
            {
                <p>You have not made any orders yet</p>
            }
        </div>
    </div>
</div>

